#cloud-config
package_update: true
packages:
  - docker.io
  - docker-compose-v2
  - git
  - rsync
runcmd:
  - [systemctl, enable, --now, docker]
  - [usermod, -aG, docker, ubuntu]
# Upload the reviewed checkout with scripts/deploy.sh after cloud-init completes.
